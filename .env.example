# Environment variables (for local development, copy to .env)
# When deploying with Docker Compose, env vars are set in docker-compose.yml

LISTEN_ADDR=127.0.0.1:9000
IMGPROXY_URL=http://127.0.0.1:48080
TEMP_DIR=/var/www/imgproxy/uploads
TOKEN_FILE=tokens.json
MAX_FILE_SIZE=20971520
READ_TIMEOUT=30s
WRITE_TIMEOUT=60s
LOG_FILE=
